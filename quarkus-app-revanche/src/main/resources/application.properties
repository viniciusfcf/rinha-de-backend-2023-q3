quarkus.http.port=9999

%dev.db_host=localhost
%prod.db_host=db

# quarkus.datasource.jdbc.url=jdbc:postgresql://${db_host}:5432/quarkus?loggerLevel=OFF
quarkus.datasource.password=quarkus
quarkus.datasource.username=quarkus
quarkus.datasource.reactive.url = vertx-reactive:postgresql://${db_host}:5432/quarkus?loggerLevel=OFF
# quarkus.datasource.jdbc.max-size=200
# quarkus.datasource.jdbc.initial-size=50
# quarkus.datasource.reactive=false
%prod.quarkus.log.console.enable=false
%prod.quarkus.log.file.enable=false

# quarkus.log.category."org.jboss.resteasy.reactive.server.handlers.ParameterHandler".level=DEBUG
# quarkus.log.category."org.jboss.resteasy.reactive.common.core.AbstractResteasyReactiveContext".level=DEBUG

quarkus.hibernate-orm.validation.enabled=false
quarkus.datasource.reactive.max-size=100
# quarkus.datasource.jdbc.reactive.max-size=100

# quarkus.datasource.jdbc.reactive.event-loop-size=50
# quarkus.datasource.reactive.event-loop-size=50
# quarkus.http.io-threads=200


# quarkus.log.min-level=TRACE
# quarkus.hibernate-orm.log.bind-parameters=true
# quarkus.hibernate-orm.log.bind-param=true
# quarkus.hibernate-orm.log.sql=true